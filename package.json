{
  "name": "iles-app",
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "devDependencies": {
    "iles": "^0.7.19",
    "remark-gfm": "^3.0.1",
    "typescript": "^4.5",
    "vue-tsc": "^0.29"
  }
}
